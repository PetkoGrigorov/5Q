package tests;

import pieces.Rook;

import java.util.Scanner;

public class RookTest {



 /*   public static void testMoveWithValidData(int row, int col, Rook testInstance) {
        Scanner cin = new Scanner(System.in);
        System.out.println();
        System.out.print("Enter movement position (row column): ");
        int moveRow = cin.nextInt();
        int moveCol = cin.nextInt();
        String message = (testInstance.isMoveValid(moveRow, moveCol)) ? "O.K." : "Fail";
        System.out.println("testMoveWithValidData: " + message);
        }

    public static void testMoveWithInvalidData(int row, int col) {
        Scanner cin = new Scanner(System.in);
        Rook testInstance = new Rook("white", row, col);
        System.out.println();
        System.out.print("Enter movement position (row column): ");
        int moveRow = cin.nextInt();
        int moveCol = cin.nextInt();
        String message = (!testInstance.isMoveValid(moveRow, moveCol)) ? "Pass" : "Fail";
        System.out.println("testMoveWithInvalidData: " + message);
    }*/






}
